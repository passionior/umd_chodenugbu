# 우면동교회 출석부 · 구글시트 연동 설치 안내

유아부·유치부·유년부·초등부·청소년부 5개 부서가 각자 로그인해서 자기 부서 출석부를
사용하고, 여러 선생님이 같은 데이터를 함께 보고 편집합니다. **구글시트를 공용 저장소**로
연결하며, 무료이고 한 번만 설정하면 됩니다.

준비물: 구글 계정 1개(교회 대표 계정 권장), 함께 받은 `index.html` 과 `Code.gs`

---

## ★ 이미 초등부용으로 쓰고 계신 경우 (업데이트)

1. Apps Script 편집기를 열어 기존 코드를 **새 `Code.gs` 내용으로 전부 교체** → 저장.
2. **배포 → 배포 관리 → (연필 ✏️ 편집) → 버전: "새 버전" → 배포**.
   반드시 "새 배포"가 아니라 **"새 버전"** 으로 하세요. 그래야 **웹앱 주소가 그대로 유지**되어
   앱에 심어둔 주소를 바꿀 필요가 없습니다.
3. GitHub의 `index.html` 도 새 파일로 덮어쓰기 하면 끝. 기존 초등부 데이터는 자동으로
   "초등부" 부서로 옮겨져 그대로 보존됩니다.

아래 A~C는 처음부터 새로 설치하는 경우의 안내입니다.

---

## A. 구글시트 백엔드 만들기 (약 5분, 한 번만)

1. **구글시트 새로 만들기** — [sheets.new](https://sheets.new) 접속. 이름을 예: `초등부 관리 데이터` 로.
2. 상단 메뉴 **확장 프로그램 → Apps Script** 클릭.
3. 편집기에 기본으로 있는 `function myFunction(){}` 내용을 **모두 지우고**,
   함께 받은 **`Code.gs` 파일의 내용을 전부 복사해 붙여넣기** → 💾 저장.
4. 오른쪽 위 **배포 → 새 배포** 클릭.
5. 톱니바퀴(⚙️ 유형 선택) → **웹 앱** 선택.
6. 설정을 이렇게 맞춥니다:
   - 실행할 사용자: **나(본인 계정)**
   - 액세스 권한이 있는 사용자: **모든 사용자** (또는 "익명 포함 모든 사용자")
7. **배포** → 처음이면 **권한 승인** 창이 뜹니다.
   "고급 → (안전하지 않음) 이동" → **허용** 을 눌러 진행하세요.
   (본인이 만든 스크립트라 안전합니다.)
8. 완료되면 나오는 **웹 앱 URL** 을 복사합니다.
   `https://script.google.com/macros/s/......./exec` 형태입니다.
   → 이 주소를 잘 보관하세요. 모든 선생님이 이 **한 개** 주소를 씁니다.

> 나중에 `Code.gs` 를 수정하면 **배포 → 배포 관리 → 편집(연필) → 버전 "새 버전" → 배포** 로 갱신하세요.
> (그래야 URL이 그대로 유지됩니다.)

---

## B. 선생님들이 폰에서 앱 열기

`초등부_학생관리.html` 은 파일 하나로 동작하는 앱입니다. 폰에서 쓰는 방법 두 가지:

**방법 1 — 링크로 열기(추천).** 앱 파일을 인터넷에 올려 링크로 만들면 모두가 편합니다.
- 가장 쉬운 무료 방법: [app.netlify.com/drop](https://app.netlify.com/drop) 에
  `초등부_학생관리.html` 파일을 끌어다 놓으면 즉시 주소가 생깁니다.
- 그 주소를 단톡방에 공유 → 선생님들은 폰 브라우저로 열고
  **"홈 화면에 추가"** 하면 앱처럼 아이콘으로 실행됩니다.

**방법 2 — 파일 전달.** 카톡 등으로 `초등부_학생관리.html` 을 보내고,
받은 선생님이 파일을 눌러 브라우저로 엽니다. (기기별로 저장은 되지만 링크 방식이 더 편합니다.)

---

## C. 앱에서 구글시트 연결 (각 선생님 폰에서 1회)

1. 앱을 열고 아래쪽 **⚙️ 설정** 탭으로 이동.
2. **🔗 구글시트 연동** 칸에 A-8에서 복사한 **웹 앱 URL** 을 붙여넣기.
3. **연결** 버튼 → 잠시 후 "✓ 동기화됨" 이 뜨면 완료입니다.
4. 다른 선생님도 같은 URL로 연결하면, 한 명이 입력한 내용이
   15초 안에 모두의 화면에 자동으로 나타납니다.

이후에는 그냥 체크만 하면 됩니다. 인터넷이 끊겨도 입력은 폰에 저장됐다가
연결되면 자동으로 반영됩니다. 상단의 작은 "동기화" 표시로 상태를 알 수 있어요.

---

## 알아두면 좋은 점

- **동시 편집 안전** — 여러 선생님이 서로 다른 학생/주를 동시에 입력해도 덮어쓰지 않고
  각 항목의 **가장 최근 입력**이 유지되도록 병합됩니다.
- **구글시트에서 바로 보기** — 연결 후에는 그 구글시트에 `학생명단`, `연락처`,
  `주간기록` 시트가 자동으로 채워져, 시트만 열어도 현황을 볼 수 있습니다.
  (이 시트를 직접 고치지는 마세요. 앱에서 입력하면 됩니다. `_data` 시트는 원본이니 건드리지 마세요.)
- **백업** — 설정 탭의 "백업 파일 저장" 으로 언제든 파일 백업을 받아둘 수 있습니다.
- **엑셀** — 통계 탭의 "엑셀 내보내기" 로 요약·주별상세·연락처를 한 파일로 받습니다.

## 문제가 생기면

- "오프라인 — 자동 재시도" 만 계속 뜬다 → URL이 정확한지, 배포 액세스가 **모든 사용자**인지 확인.
- 연결은 됐는데 데이터가 안 보인다 → 다른 선생님과 **완전히 동일한 URL** 인지 확인.
- 코드를 고친 뒤 반영이 안 된다 → **새 버전으로 다시 배포**했는지 확인.
